renv::equip()
renv::restore()

## Run the following whenever you need to build:
# drake::r_make()

## Run to visualize the drake DAG:
# drake::r_vis_drake_graph(targets_only = TRUE)
# drake::r_sankey_drake_graph(targets_only = TRUE)
